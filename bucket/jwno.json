{
  "version": "0.9.16",
  "description": "A tiling window manager for Windows 10/11, built (and scriptable) with Janet and ❤️.",
  "homepage": "https://agent-kilo.github.io/jwno",
  "license": "MIT",
  "architecture": {
    "64bit": {
      "url": "https://github.com/agent-kilo/jwno/releases/download/v0.9.16/jwno.exe"
    }
  },
  "bin": [
    "jwno.ps1",
    "jwno.exe"
  ],
  "shortcuts": [
    ["jwno.ps1", "Jwno"]
  ],
  "checkver": {
    "github": "https://github.com/agent-kilo/jwno"
  },
  "autoupdate": {
    "architecture": {
      "64bit": {
        "url": "https://github.com/agent-kilo/jwno/releases/download/v$version/jwno.exe"
      }
    }
  },
  "persist": [
    "jwno-config.janet",
    ".config\\jwno"
  ],
  "pre_install": [
    "# Create XDG-like config folder",
    "New-Item -ItemType Directory -Path \"$dir\\.config\\jwno\" -Force | Out-Null",

    "# Create wrapper script",
    "$wrapper = @'",
    "$exe = \"$PSScriptRoot\\jwno.exe\"",
    "",
    "# Pass all arguments to jwno.exe",
    "& $exe @args",
    "'@",
    "",
    "Set-Content -Path \"$dir\\jwno.ps1\" -Value $wrapper -Encoding UTF8"
  ],
  "notes": [
    "The PowerShell wrapper (jwno.ps1) is the main entry point and supports command-line arguments.",
    "Run 'jwno -c path/to/config.janet' to use a specific config file.",
    "Run 'jwno --help' to see all available command-line options.",
    "",
    "Alternative: You can also run 'jwno.exe' directly with command-line arguments.",
    "",
    "Recommended location for your config:",
    "  ~/scoop/apps/jwno/current/jwno-config.janet",
    "  or",
    "  ~/.config/jwno/config.janet",
    "",
    "Your config file(s) are persisted across updates."
  ]
}
